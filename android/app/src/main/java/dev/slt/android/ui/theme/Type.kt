package dev.slt.android.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.font.FontFamily

/**
 * SLT typography tokens.
 *
 * Built on the Material 3 default type scale; a dedicated monospace family is
 * provided for raw-text surfaces (TOML editor, logs, config fields).
 */

val Mono: FontFamily = FontFamily.Monospace

internal val SltTypography = Typography()
